// ImageView.js

var { requireNativeComponent, PropTypes } = require('react-native');

var iface = {
  name: 'NearFieldCommunications',
  propTypes: {
  },
};

module.exports = requireNativeComponent('NearFieldCommunications', iface);